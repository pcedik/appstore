<% if can? :manage, @aplikace %>
	<%= link_to 'Administrace', rails_admin.dashboard_path %>
<% end %>

<% 
   @aplikaces.each do |aplikace| 
		@aplikace = aplikace 
%>

		<%= render 'show' %>

	<% end %>